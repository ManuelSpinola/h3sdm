% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3sdm_fit_models_stack.R
\name{h3sdm_fit_models_stack}
\alias{h3sdm_fit_models_stack}
\title{Fit multiple workflows for model stacking}
\usage{
h3sdm_fit_models_stack(workflows, data_split, presence_data = NULL)
}
\arguments{
\item{wfs}{A named list of tidymodels workflows. Each element should be a workflow object.}

\item{scv}{A resampling object, e.g., generated by \code{rsample::vfold_cv()}.}

\item{datos_presencia}{A data frame containing the data to fit the workflows on.}
}
\value{
A list with one element \code{models}. Each element of \code{models} is itself
a list with:
\itemize{
\item \code{cv_model}: the resampled model (\code{tune_results} object)
\item \code{workflow}: the original workflow object
}
}
\description{
Fits a set of tidymodels workflows using resampling with \code{fit_resamples()}
and \code{control_stack_resamples()} to generate objects suitable for stacking.
For each workflow, both the resampled model (\code{cv_model}) and the original
workflow are returned. This is intended as a helper function for creating
ensembles of multiple models in the \code{h3sdm} workflow.
}
\details{
Fit multiple workflows with resampling for stacking
}
\examples{
\dontrun{
# Assuming you have workflows wfs and a vfold_cv object scv
results <- h3sdm_fit_models_stack(wfs, scv, datos_presencia)
results$models$random_forest$cv_model
results$models$random_forest$workflow
}

}
